# 📘 **Задание: Иерархия умных устройств “SmartHome”**

Создай систему классов для управления “умным домом”.

## 🎯 **Базовый класс: `SmartDevice`**

У каждого устройства должны быть общие поля:

* `String id` — уникальный идентификатор
* `String name` — имя
* `bool isOnline` — в сети или нет

Методы:

* `void turnOn()`
* `void turnOff()`
* `void reboot()` — в базовой реализации делает `turnOff()` → `turnOn()`

### 🔹 **Требования к конструкторам:**

1. **Основной конструктор** принимает все поля.
2. **Именованный конструктор `SmartDevice.defaultNamed()`**

   * задаёт `isOnline = true`
   * остальные поля принимает как параметры.
3. **Фабричный конструктор `SmartDevice.create(Map<String, dynamic> json)`**

   * должен создавать **разные подклассы** в зависимости от `json['type']`:

     * `"light"` → `SmartLight`
     * `"thermo"` → `SmartThermostat`
     * `"cam"` → `SmartCamera`
   * если тип неизвестен → выбросить ошибку.

---

## 💡 **Класс `SmartLight`** (наследуется от `SmartDevice`)

Дополнительные поля:

* `int brightness` (0..100)
* `String color`

Требования:

* Имеет **свой именованный конструктор**:
  `SmartLight.whiteDefault(...)`
  — где `color = "white"` и `brightness = 100` по умолчанию.

* Переопределяет метод `reboot()` так, чтобы:

  * если яркость была ниже 20 → перед перезагрузкой яркость устанавливается на 20.

---

## 🌡 **Класс `SmartThermostat`**

Дополнительные поля:

* `double temperature`
* `bool ecoMode`

Требования:

* Имеет **конструктор, который вызывает именованный конструктор базового класса**.
* Переопределяет `turnOn()` так, чтобы включение автоматически отключало `ecoMode`.

---

## 📹 **Класс `SmartCamera`**

Дополнительные поля:

* `bool isRecording`
* `int storageMb`

Требования:

* Имеет фабричный конструктор `SmartCamera.fromStorage(int storage)`
  — если storage < 500 → вернуть **объект дочернего класса `SmartMiniCamera`**
  — иначе вернуть обычный `SmartCamera`.

* Переопределяет `turnOff()` так, чтобы при выключении запись останавливалась.

---

## 🎥 **Класс `SmartMiniCamera`** (наследуется от `SmartCamera`)

Дополнительные особенности:

* `storageMb` всегда ограничен 256
* Имеет свой именованный конструктор, который вызывает **именованный базового класса**, а затем ограничивает storage.

---

## 🧪 **Тестовая часть задания**

Без написания кода, просто требования:

Создай список устройств, используя:

* обычные конструкторы
* именованные конструкторы
* фабричные конструкторы (и базового, и камеры)

А затем:

* Выполни `turnOn()`, `turnOff()`, `reboot()` на каждом устройстве в цикле.
* Убедись, что переопределённые методы реально вызываются.

---

## 🔥 **Дополнительное усложнение (не обязательно)**

Добавить класс:

### **`DeviceGroup`**

* хранит список `SmartDevice`
* имеет метод `applyToAll(void Function(SmartDevice d) action)`
* позволяет выполнять действия на всех устройствах.

---