# 🧩 **ЗАДАНИЕ 1 — Базовый класс с обычным конструктором + геттеры/сеттеры**

Создай класс **User**, который хранит:

* `String name`
* `int age`
* `String _password` — приватное поле

Требования:

1. Сделай **обычный конструктор**, принимающий `name` и `age`, но **не принимающий пароль**.
2. Создай **сеттер для пароля**, который:

   * принимает строку,
   * отклоняет пароль короче 6 символов,
   * записывает его только если он подходит.
3. Создай **геттер password**, который возвращает `"***"` (маску), а настоящий пароль возвращает через метод `getRawPassword()`.

---

# 🧩 **ЗАДАНИЕ 2 — Именованные конструкторы**

Расширь класс User:

Создай именованный конструктор:

### `User.guest()`

Который создаёт гостя:

```dart
name = 'Guest'
age = 0
```

А пароль не задаёт.

---

# 🧩 **ЗАДАНИЕ 3 — Класс с фабричным конструктором + кэширование объектов**

Создай класс **City**, который:

* имеет `String name`
* имеет `int population`

Требования:

1. Сделай **обычный именованный конструктор** `City.create(this.name, this.population)`
2. Сделай **фабричный конструктор**:

```dart
factory City(String name, int population)
```

Который:

* хранит уже созданные города в **статической Map<String, City> _cache**
* если город с таким именем уже есть — возвращает его
* иначе создаёт новый через `City.create(...)`, кладёт в `_cache` и возвращает

То есть ты делаешь **кэшируемый фабричный конструктор**.

---

# 🧩 **ЗАДАНИЕ 4 — Хешируемый класс**

Сделай так, чтобы:

```dart
final c1 = City('Paris', 2000000);
final c2 = City('Paris', 2000000);
print(c1 == c2);    // true
print(c1.hashCode == c2.hashCode); // true
```

Требования:

* переопредели `==` и `hashCode`
* сравнивай только по полю `name` (население можно игнорировать)

---

# 🧩 **ЗАДАНИЕ 5 — Хешируемый Singleton**

Создай класс **Settings**, который должен быть:

* синглтоном,
* иметь hashCode и ==,
* содержать поля:

  * `String theme`
  * `int fontSize`

Задание:

1. Сделай **приватный именованный конструктор** `Settings._internal()`
2. Внутри класса создай:

```dart
static final Settings _instance = Settings._internal();
```

3. Реализуй **factory Settings()**, который всегда возвращает `_instance`.
4. Добавь **геттеры/сеттеры** для полей.
5. Сделай так, чтобы сравнение двух вызовов было истинным:

```dart
print(Settings() == Settings()); // true
```

6. Переопредели:

* оператор `==`, чтобы сравнение шло именно по ссылке (как обычно у синглтонов),
* `hashCode`, чтобы был стабильным (можно вернуть любое фиксированное число).

---

# 🧩 **ЗАДАНИЕ 6 — Общий проект на все темы**

Собери мини-приложение:

## "Система пользователей и городов"

* есть список пользователей (класс User),
* каждый пользователь может быть приписан к одному городу (City),
* настройки приложения (Settings) — singleton,
* создай несколько пользователей, городов, назначь город каждому,
* выведи пользователей в виде:

```
User: Alex (21)
City: Paris
Theme: dark
Font size: 16
```
