# 🧩 **Практическое задание: “Математическая библиотека Vector & BitMask”**

Ты создаёшь мини-библиотеку, содержащую два класса:

1. **Vector2D** — двухмерный вектор.
2. **BitMask** — класс для работы с битовыми флагами.

Оба класса должны активно использовать **перегрузку операторов**, а библиотека — **статические поля/методы** для хранения общей статистики.

---

# ✏️ **Часть 1. Класс Vector2D с перегрузкой операторов**

Создай класс:

```dart
class Vector2D {
  final double x;
  final double y;

  static int createdVectors = 0;

  Vector2D(this.x, this.y) {
    createdVectors++;
  }
}
```

### ✔️ Твои задачи:

### 1) Перегрузи операторы:

* `+`  — сложение векторов
* `-`  — вычитание
* `*`  — умножение вектора на число
* `==` — сравнение по равенству
* `[]` — доступ к координате по индексу:

  * `vector[0]` → x
  * `vector[1]` → y
* `call()` — чтобы можно было вызвать вектор как функцию:

  ```dart
  final v = Vector2D(3, 4);
  v(); // должен возвращать длину вектора
  ```

### 2) Добавь статический метод:

```dart
static void showStats() {
  print("Создано векторов: $createdVectors");
}
```

### 3) Добавь метод length (но call должен использовать его)

---

# ✏️ **Часть 2. Класс BitMask с битовыми операторами**

Создай класс:

```dart
class BitMask {
  int value;
  static int operationsCount = 0;

  BitMask(this.value);
}
```

### ✔️ Перегрузи операторы:

* `|` — побитовое OR
* `&` — побитовое AND
* `^` — XOR
* `<<` — сдвиг влево
* `>>` — сдвиг вправо
* `~` — инверсия (унарный оператор)

Каждый оператор должен увеличивать `operationsCount++`.

Пример:

```dart
final a = BitMask(5);
final b = BitMask(3);
final c = a | b; // value = 7
```

### ✔️ Добавь статический метод:

```dart
static void showStats() {
  print("Кол-во битовых операций: $operationsCount");
}
```

---

# ✏️ **Часть 3. Маленькие тесты (как мини-проект)**

Напиши функцию `main()` и проверь:

### 1) Vector2D:

* создай 3–4 вектора
* выполни сложение, вычитание
* умножь на число
* сравни два вектора
* получи координаты через `[]`
* вызови вектор как функцию: `print(v());`
* вызови `Vector2D.showStats()`

### 2) BitMask:

* создай несколько масок
* проведи все битовые операции
* распечатай значения
* вызови `BitMask.showStats()`

---

# ⭐ **Дополнительное необязательное задание (повышенная сложность)**

Сделай в Vector2D *неизменяемую* длину, которая кешируется:

### требования:

* вычисляется один раз
* хранится в `final` поле
* но оператор `+` или `-` должен создавать новый Vector2D с новым кэшом