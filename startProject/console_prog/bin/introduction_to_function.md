# 🚀 **Задания по функциям в Dart**

## **1. Позиционные аргументы**

### **Задание 1**

Напиши функцию

```dart
int sum(int a, int b)
```

которая возвращает сумму двух чисел.

### **Задание 2**

Напиши функцию

```dart
String repeat(String text, int n)
```

которая возвращает строку `text`, повторённую `n` раз.

---

## **2. Позиционные опциональные аргументы**

### **Задание 3**

Создай функцию

```dart
String buildName(String first, [String? last])
```

Если `last` передано — возвращай `"first last"`, если нет — `"first"`.

### **Задание 4**

Функция

```dart
double power(double base, [int exp = 2])
```

Возвращает `base` в степени `exp`.
Если степень не передана — возводи в квадрат.

---

## **3. Именованные аргументы**

### **Задание 5**

Функция

```dart
void greet({required String name, int age = 18})
```

Печатает:
`"Привет, name! Тебе age лет."`

Попробуй вызвать её:

* только с `name`
* с `name` и `age`

### **Задание 6**

Функция

```dart
String formatUrl({required String host, String scheme = 'https', int port = 443})
```

Возвращает строку вида:
`scheme://host:port`

---

## **4. Именованные nullable + default**

### **Задание 7**

Напиши функцию

```dart
String describe({String? city, String country = 'Россия'})
```

* Если `city == null` → возвращает: `"Страна: country"`
* Иначе → `"Город: city, страна: country"`

---

## **5. Смешанные аргументы**

### **Задание 8**

Функция

```dart
int calc(int a, int b, {bool absolute = false})
```

* Если `absolute = true`, возвращай абсолютную разницу между числами.
* Если `false` → обычную разницу `a - b`.

### **Задание 9**

Функция

```dart
String fullName(String first, String last, {String? middle})
```

Собери:

* если middle есть → `"first middle last"`
* если нет → `"first last"`

---

## **6. Аргументы, которые могут быть null**

### **Задание 10**

Функция

```dart
int safeLength(String? text)
```

Возвращает длину строки или `0`, если строка `null`.

### **Задание 11**

Функция

```dart
bool containsDigit(String? value)
```

Если строка null → возвращает `false`.
Иначе проверяет, есть ли в строке хотя бы одна цифра.

---

## **7. Функции высшего порядка + аргументы**

### **Задание 12**

Функция

```dart
int apply(int a, int b, int Function(int, int) op)
```

Принимает два числа и функцию-операцию.
Примеры вызова:

* `apply(2, 3, (x, y) => x + y)`
* `apply(4, 2, (x, y) => x ~/ y)`

---

## **8. Уровень PRO**

### **Задание 13**

Сделай функцию с такими особенностями:

* принимает обязательный позиционный аргумент `String path`
* принимает именованный nullable аргумент `String? ext`
* принимает именованный аргумент `bool withSlash` со значением по умолчанию `false`

Если `withSlash == true`, добавь `/` в конце
Если `ext != null`, добавь `.$ext`
Пример:
`buildPath("folder/file", ext: "txt", withSlash: true)`
→ `"folder/file.txt/"`

### **Задание 14**

Напиши функцию

```dart
dynamic choose(bool flag, {required dynamic a, required dynamic b})
```

Если `flag == true` → вернуть `a`, иначе `b`.

---

## **9. Комбинация всех типов аргументов**

### **Задание 15**

Функция

```dart
String userInfo(
  String username,
  int id,  
  {String? phone, 
   String role = 'user',
   bool? emailConfirmed}
)
```

Возвращает строку:

```
Имя: username
ID: id
Телефон: phone или "не указан"
Роль: role
Email подтверждён: да/нет/неизвестно
```