## 🧠 Практическое задание: «Файловый процессор логов»
### 🎯 Цель

Научиться:

* читать файлы **разными способами** (sync / async / stream),
* писать в файлы,
* работать с большими файлами,
* обрабатывать ошибки,
* понимать **когда какой способ оправдан**.

---

## 🗂 Исходные данные

Создай файл `input.log` со следующим содержимым (или сгенерируй сам):

```
INFO|2025-01-01 10:00:01|User login
WARN|2025-01-01 10:01:12|Invalid password
ERROR|2025-01-01 10:02:45|Database unavailable
INFO|2025-01-01 10:03:00|User logout
ERROR|2025-01-01 10:04:11|Timeout exceeded
```

Формат строки:

```
<LEVEL>|<TIMESTAMP>|<MESSAGE>
```

---

## 🧩 Часть 1 — Синхронное чтение

### Задание

1. Прочитай файл **целиком** синхронно.
2. Распарсь строки.
3. Подсчитай:

   * сколько `INFO`
   * сколько `WARN`
   * сколько `ERROR`

📌 Использовать:

* `File.readAsStringSync()`

📤 Вывод в консоль:

```
INFO: 2
WARN: 1
ERROR: 2
```

---

## 🧩 Часть 2 — Асинхронное чтение

### Задание

Сделай **то же самое**, но:

* чтение файла должно быть асинхронным,
* `main` должен быть `async`.

📌 Использовать:

* `await File.readAsString()`

⚠️ Обрати внимание:

* чем отличается сигнатура `main`
* где нужен `await`

---

## 🧩 Часть 3 — Потоковое чтение (ключевая часть)

### Задание

Прочитай файл **построчно**, не загружая его целиком в память.

📌 Использовать:

* `File.openRead()`
* `utf8.decoder`
* `LineSplitter`

🧠 Вопрос, на который ты должен ответить сам:

> Почему этот способ предпочтительнее для файлов 1–10 ГБ?

---

## 🧩 Часть 4 — Фильтрация и запись

### Задание

1. Отфильтруй **только `ERROR` записи**.
2. Запиши их в файл `errors.log`.

📌 Условия:

* запись должна быть **асинхронной**
* формат строк сохранить

📌 Использовать:

* `File.writeAsString(..., mode: FileMode.write | append)`

---

## 🧩 Часть 5 — Обработка ошибок

### Задание

Обработай ситуации:

* файл не существует
* нет прав на запись
* файл пустой

📌 Использовать:

* `try / catch`
* `FileSystemException`

---

## 🧩 Часть 6 — Мини-исследование (важно!)

Ответь **в комментариях к коду**:

1. Когда **НЕ стоит** использовать `readAsStringSync()`?
2. Чем `openRead()` принципиально отличается от `readAsString()`?
3. Почему `await` внутри цикла — потенциальная проблема?
4. В каком месте программа реально становится **неблокирующей**?

---

## 🔥 Бонус (по желанию)

* Добавь аргумент командной строки:

  ```
  dart run main.dart input.log errors.log
  ```
* Если аргументов нет — выбрасывай кастомное исключение `InvalidArgumentsException`.

---

## 🏁 Критерий «я реально понял тему»

Ты **можешь без шпаргалок объяснить**:

* разницу между `sync / async / stream`
* почему стримы — это не просто «медленнее»
* где Dart блокирует поток, а где нет