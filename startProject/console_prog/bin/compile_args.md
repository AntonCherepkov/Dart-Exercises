## 🎯 Цель тренировки

1. Разобраться **как по-разному компилируется Dart**
2. Научиться **передавать аргументы при запуске**
3. Освоить **парсинг аргументов** (`args`, флаги, значения)
4. Понять различия поведения **VM / AOT / JS**

---

## 🧱 Условие задачи

Ты пишешь консольную утилиту **`envinfo`**, которая:

* принимает аргументы запуска
* выводит информацию о режиме запуска
* ведёт себя немного по-разному в зависимости от аргументов

---

## 📁 Структура проекта

```
envinfo/
 ├─ bin/
 │   └─ envinfo.dart
 └─ pubspec.yaml
```

---

## 📌 Часть 1. Аргументы запуска (база)

### 🔹 Требования

Программа принимает аргументы:

| Аргумент   | Тип   | Описание             |
| ---------- | ----- | -------------------- |
| `--name`   | value | имя пользователя     |
| `--age`    | value | возраст              |
| `--debug`  | flag  | включает debug-режим |
| `--format` | value | `text` или `json`    |

---

### 🔹 Пример запуска

```bash
dart run bin/envinfo.dart --name Anton --age 33 --debug --format json
```

---

### 🔹 Минимальное поведение

* Без аргументов → сообщение об использовании
* `--debug` → печатает **сырые args**
* `--format text` → обычный текст
* `--format json` → JSON-вывод

---

## 📌 Часть 2. Парсинг аргументов (ручками)

❗ **Без сторонних пакетов** (`args` нельзя)

### Требуется:

* Использовать `List<String> args`
* Распознать:

  * `--key value`
  * флаги (`--debug`)
* Обработать ошибки:

  * неизвестный аргумент
  * отсутствующее значение

📎 Подсказка:

```dart
for (var i = 0; i < args.length; i++) {
  final arg = args[i];
}
```

---

## 📌 Часть 3. Компиляция и запуск (главное)

### 🔹 1. Запуск в VM (JIT)

```bash
dart run bin/envinfo.dart --name Anton
```

➡️ Ответь себе:

* Когда происходит компиляция?
* Можно ли менять код без пересборки?

---

### 🔹 2. Компиляция в AOT (exe)

```bash
dart compile exe bin/envinfo.dart -o envinfo
```

Запуск:

```bash
./envinfo --name Anton --debug
```

➡️ Проверь:

* Работают ли аргументы так же?
* Быстрее ли старт?

---

### 🔹 3. Компиляция в JS

```bash
dart compile js bin/envinfo.dart -o envinfo.js
```

Запуск:

```bash
node envinfo.js --name Anton
```

➡️ Подумай:

* Какие ограничения появляются?
* Есть ли доступ к `Platform`?

---

## 📌 Часть 4. Задание со звёздочкой ⭐

### Добавь аргумент:

```
--mode dev|prod
```

Поведение:

* `dev` → разрешён `--debug`
* `prod` → если передан `--debug` → `throw Exception`

---

## 📌 Часть 5. Контрольные вопросы (обязательно)

Ответь **после выполнения**, не сейчас 😉

1. Чем `dart run` отличается от `dart compile exe`?
2. В каком режиме:

   * быстрее старт?
   * больше размер?
3. Отличаются ли `args` в VM и AOT?
4. Почему `dart compile js` — не просто «ещё один бинарник»?

---

## 🧠 Итог

После этого задания ты:

* **чётко понимаешь** все способы компиляции Dart
* умеешь **писать CLI без пакетов**
* знаешь, **что реально передаётся в `args`**
* понимаешь, **где Dart ведёт себя по-разному**

---