# 🟩 **Уровень 1 — Базовые**

### **1. Чётные числа в диапазоне**

Создай синхронный генератор, который принимает `start` и `end` и выдаёт только чётные числа в диапазоне.

**Пример:**

```dart
print(evens(1, 10).toList()); // [2, 4, 6, 8, 10]
```

---

### **2. Генератор символов строки**

Напиши генератор, который принимает строку и выдаёт её символы по одному.

**Пример:**

```dart
for (var ch in chars("dart")) print(ch);
// d a r t
```

---

# 🟨 **Уровень 2 — Средние**

### **3. Разворачивание списка**

Сделай генератор, который принимает вложенный список **любой вложенности**, где элементы — либо `int`, либо `List`, и выдаёт все `int` последовательно.

Пример входных данных:

```dart
[1, [2, 3], [4, [5, 6]], 7]
```

Ожидаемый вывод:

```
1, 2, 3, 4, 5, 6, 7
```

> Используй `yield*` чтобы развернуть вложенные генераторы.

---

### **4. Генератор процентов**

Функция принимает `value` и выдаёт шаги увеличения от 0% до 100% с заданным шагом, например `10`.

```dart
for (var v in percentValue(250, step: 25)) {
  print(v);
}
```

Ожидаемый вывод:

```
0, 62.5, 125, 187.5, 250
```

---

# 🟧 **Уровень 3 — Продвинутые**

### **5. Генератор бесконечной последовательности Фибоначчи (sync)**

Сделать генератор, который выдаёт Фибоначчи бесконечно, но прекращается, если пользователь делает `take(n)`.

```dart
fib().take(10).forEach(print);
```

---

### **6. Генератор "скользящего окна"**

Напиши генератор, который принимает список и окно `k`, и для каждого шага выдаёт подпоследовательность длиной `k`.

**Пример:**

```
input: [1,2,3,4,5], k=3
выход:
[1,2,3]
[2,3,4]
[3,4,5]
```

---

# 🟥 **Уровень 4 — Хардкор**

### **7. Мини-парсер математических выражений**

Напиши генератор, который принимает строку вида:

```
"10 + 20 - 5 + 3"
```

и выдаёт токены:

```
10
+
20
-
5
+
3
```

> Разбор через sync* + пошаговое чтение строки.

---

### **8. Генератор flatten для дерева**

Есть структура:

```dart
class Node {
  final int value;
  final List<Node> children;
  Node(this.value, [this.children = const []]);
}
```

Сделай генератор, который обходит дерево в **глубину** и выдаёт все числа.
