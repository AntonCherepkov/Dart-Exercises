# 🚀 **ЗАДАНИЕ**

## 1️⃣ Создай два typedef:

### ➤ **NumberOp** — принимает `double`, возвращает `double`

### ➤ **Validator** — принимает `double`, возвращает `bool`

---

## 2️⃣ Напиши функцию:

```
double processNumber(
  double value,
  List<NumberOp> operations,
  List<Validator> validators,
)
```

### Что она должна делать:

1. **Перед тем как применить операции, проверить число всеми валидаторами.**
   Если **хоть один валидатор вернул false** →
   выбросить исключение **ArgumentError("Invalid number")**

2. **Если всё ок — применить операции по порядку.**

3. **Вернуть итоговое число.**

---

## 3️⃣ Условия:

* Валидации и операции должны работать **чисто как функции**.
* Исключение должно выкидываться строго до применения операций.
* Никаких print — только чистое функциональное поведение.
* Реализуй всё без подсказок — typedef + функции + вызов.

---

## 🎯 Пример использования:

Ты сам должен создать валидаторы, операции и протестировать.

---