
## 📝 **Задание**

**1.** Создай три функции:

* `add(a, b)` — возвращает сумму
* `sub(a, b)` — возвращает разность
* `mul(a, b)` — возвращает произведение

**2.** Создай словарь `ops`, где ключи — строки `"+"`, `"-"`, `"*"`, а значения — **сами функции** (`add`, `sub`, `mul`).

**3.** Напиши функцию `calculate(op, a, b)`, которая:

* принимает строку операции
* достает нужную функцию из словаря
* вызывает её и возвращает результат

Пример использования:

```python
print(calculate("+", 10, 5))  # 15
print(calculate("*", 7, 6))   # 42
```

---