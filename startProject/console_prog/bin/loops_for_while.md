# 📝 **Задание: “Анализ заказов клиентов”**

Представь, что у тебя есть список клиентов и их заказы:

```dart
final clients = [
  {
    'name': 'Anton',
    'orders': [1200, 800, 300]
  },
  {
    'name': 'Maks',
    'orders': [150, 200]
  },
  {
    'name': 'Olga',
    'orders': []
  },
  {
    'name': 'Dina',
    'orders': [999, 1500, 2000, 50]
  },
];
```

---

# 🎯 **Твоя задача**

С использованием **всех типов циклов (for, for-in, while, do-while)** нужно:

---

## **1️⃣ Цикл `for`**

Пройтись по списку `clients` обычным `for`:

* взять каждую мапку
* выполнить **деструктурирование**:

```dart
var {'name': name, 'orders': orders} = clients[i];
```

* вывести имя клиента и количество заказов.

---

## **2️⃣ Цикл `for-in`**

Снова пройтись по списку, но уже через `for-in`,
и:

* вычислить общую сумму заказов каждого клиента
* вывести:

```
Anton потратил 2300 руб.
```

---

## **3️⃣ Цикл `while`**

С помощью `while`:

* пройтись по каждому клиенту
* для каждого клиента пройтись по списку покупок тоже через `while`
* найти **максимальный заказ**
  (например, у Dina это 2000)

Вывести:

```
Максимальный заказ у Dina: 2000
```

---

## **4️⃣ Цикл `do-while`**

Сделать подсчёт:

* сколько клиентов сделали **хотя бы один заказ**
* сколько клиентов **не сделали ни одного**

Работать именно с `do-while`.

Вывести:

```
С заказами: X
Без заказов: Y
```

---

# ⭐ Дополнительное условие (чтобы было красиво)

При деструктурировании используй запись:

```dart
var {'name': name, 'orders': List orders} = client;
```

И для списка заказов можно применить деструктурирование элементов:

```dart
for (var order in orders) {
  ...
}
```

---

# 🏁 **Итоговый результат**

В конце программа должна вывести:

* список клиентов с количеством заказов
* сумму заказов каждого
* максимальный заказ каждого
* статистику по количеству клиентов с заказами и без

---
