## 🎯 Задание: «Сервис авторизации»
### 📌 Условие

Реализуй **мини-сервис авторизации пользователя**, который:

* принимает логин и пароль,
* валидирует входные данные,
* выбрасывает **пользовательские исключения**,
* корректно **обрабатывает ошибки**.

---

## 🧩 Требования
### 1️⃣ Пользовательские исключения

Создай **иерархию исключений**:

```dart
abstract class AuthException implements Exception {
  final String message;
  AuthException(this.message);
}
```

И минимум **3 конкретных исключения**:

* `EmptyCredentialsException`
* `InvalidPasswordException`
* `UserNotFoundException`

👉 каждое должно иметь **свой осмысленный текст ошибки**

---

### 2️⃣ Класс UserService

Создай класс:

```dart
class UserService {
  final Map<String, String> _users = {
    'admin': '123456',
    'user': 'qwerty',
  };

  void login(String? username, String? password) {
    // реализация
  }
}
```

#### Логика метода `login`:

* если `username == null` или `password == null` → `EmptyCredentialsException`
* если пользователя нет в `_users` → `UserNotFoundException`
* если пароль неверный → `InvalidPasswordException`
* если всё ок → вывести в консоль:

  ```
  Login successful: <username>
  ```

---

### 3️⃣ Обработка ошибок

В `main()`:

```dart
void main() {
  final service = UserService();

  try {
    service.login('admin', '123');
  } on AuthException catch (e) {
    print('Auth error: ${e.message}');
  } catch (e) {
    print('Unknown error: $e');
  } finally {
    print('Auth attempt finished');
  }
}
```

---

## ⭐ Доп. уровень (по желанию, но кайфовый)

* Переопредели `toString()` у исключений
* Добавь `enum AuthErrorCode`
* Сделай так, чтобы исключение хранило **код + сообщение**
* Добавь логирование ошибок (хотя бы `print`)

---

## 🧠 Что ты закрепишь

✔ `throw` vs `Exception`
✔ пользовательские классы ошибок
✔ `try / on / catch / finally`
✔ иерархию исключений
✔ чистую архитектуру обработки ошибок