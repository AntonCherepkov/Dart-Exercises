# 💡 Задание 1 на замыкание

Сделай функцию **counter**, которая **создаёт и возвращает другую функцию**, и эта внутренняя функция должна каждый раз увеличивать число на 1.

Ожидаемое поведение:

```dart
void main() {
  var c1 = counter();  
  print(c1()); // 1
  print(c1()); // 2
  print(c1()); // 3

  var c2 = counter();
  print(c2()); // 1   (отдельный счётчик)
  print(c2()); // 2
}
```

### Задача:

Реализовать `counter()` так, чтобы через замыкание хранилось внутреннее состояние счётчика.

Подсказка:

* внутри counter держи локальную переменную,
* возвращай функцию, которая её модифицирует.

# 🚀 Задание 2 на замыкание

## **Сделай фабрику функций, которая создаёт "ограниченный счётчик"**

Требования:

1. Функция `makeLimitedCounter` принимает:

   * `start` — начальное значение
   * `step` — на сколько увеличивать
   * `limit` — максимальное значение

2. Возвращает функцию-счётчик, которая:

   * прибавляет `step`
   * но **если превышает limit — сбрасывается на start**

Пример использования:

```dart
void main() {
  var c = makeLimitedCounter(start: 0, step: 2, limit: 5);

  print(c()); // 0  (первый вызов возвращает start)
  print(c()); // 2
  print(c()); // 4
  print(c()); // 0  (превышаем limit -> сброс)
  print(c()); // 2
}
```

## 🎯 Задача:

Реализовать `makeLimitedCounter` так, чтобы всё работало через **замыкание** — никакие классы, только функция, возвращающая функцию.

Подсказка:

* нужно хранить **текущее значение** внутри замыкания
* нужно проверять превышение лимита
* сбрасывать, когда нужно
